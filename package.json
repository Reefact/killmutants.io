{
  "name": "killmutants-io",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The killmutants.io website.",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "node scripts/generate-version.mjs && node scripts/generate-release-note.mjs && pnpm --filter @killmutants/web dev",
    "build": "node scripts/build-site.mjs",
    "check": "node scripts/generate-version.mjs && node scripts/generate-release-note.mjs && pnpm --filter @killmutants/web check",
    "serve": "wrangler dev",
    "preview": "wrangler versions upload",
    "deploy": "wrangler deploy",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "serve": "^14.2.6",
    "typescript": "^5.9.3",
    "wrangler": "^4.127.0"
  }
}
