/** Facts about killmutants.io and the tool it announces, kept in one place rather than
 *  spelled out again wherever a page needs one. */
export const site = {
  name: 'KillMutants',

  /** The tool's own repository — not this site's. */
  repository: 'https://github.com/Reefact/kill-mutants',

  /** This site's own repository, as opposed to the tool's above. */
  siteRepository: 'https://github.com/Reefact/killmutants.io',
};
